source "https://rubygems.org"

# HTML renderer for the published documents (document.mnd.html).
#
# Pinned to the released 0.5.x line: the git main branch cannot render
# standoc-family (iho) documents yet — metanorma-standoc's root model is
# mid-migration and metanorma-iho@feat/move-iho-document's gemspec still
# targets the relaton 2.x generation. Flip to `github: "metanorma/metanorma-document", branch: "main"` once those land.
gem "metanorma-document", "~> 0.5.1"
